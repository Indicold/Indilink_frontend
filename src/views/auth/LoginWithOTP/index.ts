/* 
* This code is importing the `LoginWithOTP` module from the file "./LoginWithOTP" and then exporting
* it as the default export. This means that when another module imports this module, they can simply
* use `import LoginWithOTP from "./LoginWithOTP"` to access the `LoginWithOTP` module. 
*/
import LoginWithOTP from "./LoginWithOTP"
export default LoginWithOTP