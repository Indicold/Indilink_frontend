/* This code is importing the `ResetPassword` component from the `ResetPassword` file and exporting it
as the default export. This means that when this module is imported in another file, the
`ResetPassword` component can be accessed using the default import syntax. */
import ResetPassword from './ResetPassword'

export default ResetPassword
