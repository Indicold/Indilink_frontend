/* 
*  This code is importing the `ForgotPassword` component from the `./ForgotPassword` file and then
*  exporting it as the default export. This means that when this module is imported elsewhere, the
*  `ForgotPassword` component will be the default export and can be accessed using the imported
*  module's name. 
*/
import ForgotPassword from './ForgotPassword'

export default ForgotPassword
