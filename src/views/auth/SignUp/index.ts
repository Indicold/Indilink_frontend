/* This code is importing the `SignUp` module from the file `SignUp.ts` and then exporting it as the
default export. This means that when another module imports this module, they can simply use `import
SignUp from './SignUp'` to access the `SignUp` module. */
import SignUp from './SignUp'

export default SignUp
