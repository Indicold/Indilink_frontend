/* 
* This code is importing the `MultistepForm` component from the file "./MultistepForm" and then
* exporting it as the default export of this module. This means that when another module imports this
* module, they can access the `MultistepForm` component by importing it as the default export. 
*/
import MultistepForm from "./MultistepForm";

export default MultistepForm;