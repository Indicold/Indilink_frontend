/* 
* This code is importing the `BasicInformation` module from the file `BasicInformation.ts` and then exporting it as
* the default export of the current module. 
* This means that any other module that imports this module will receive the `BasicInformation` module as the
* default export. 
*/
import BasicInformation from './BasicInformation'

export default BasicInformation;