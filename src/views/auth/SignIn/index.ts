/* This code is importing the `SignIn` module from the file `./SignIn` and then exporting it as the
default export. This means that when another module imports this module, they can access the
`SignIn` module directly without having to specify its name. */
import SignIn from './SignIn'

export default SignIn
